# standards-sdk module info

Minimal example that imports `github.com/hashgraph-online/standards-sdk-go/standards-sdk` and prints module metadata.

Run:

```bash
go run ./examples/standards-sdk-discovery
```
